import React from 'react';
import ReactDOM from 'react-dom';
import LoadOnLastFiveExample from './DemoApp';

ReactDOM.render(
  <React.StrictMode>
    <LoadOnLastFiveExample />
  </React.StrictMode>,
  document.getElementById('root'),
);
